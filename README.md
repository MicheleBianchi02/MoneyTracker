## MoneyTracker

# Dev

To install the source code, just clone the repo and run

```
pip install -e .
```

make shure to have a virtual enviroment already created.
If not all the dependencies are present, just install them using pip
